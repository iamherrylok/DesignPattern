简单工厂的不足：
若要增加一个具体类，就需要更改工厂方法的代码，违反开闭原则；

抽象类：
1.不能被实例化；
2.抽象类可拥有抽象方法和普通方法，普通类不可以含有抽象方法；
3.抽象方法不可有方法体；
4.抽象类的派生类必须重写（override）抽象方法，或者派生类同样是抽象类；
5.抽象类不能被密封；

接口:
1.接口不能被实例化;
2.接口的方法不能有方法体;
3.实现接口的类必须实现接口方法；
4.类可以实现多个接口；

抽象类和接口的区别：
